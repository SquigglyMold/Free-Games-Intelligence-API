# Server port (optional; default 3000). Vercel sets PORT automatically.
PORT=3000

# Add any API keys or config here when needed (e.g. future services).
# Set these in Vercel: Project → Settings → Environment Variables.
# EXAMPLE_API_KEY=
